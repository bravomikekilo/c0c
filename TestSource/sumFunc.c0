int sum(int n) {
    int i, sum;
    i = -0;
    sum = 0;
    do {
        sum = sum + i * i;
        i = i + 1;
    } while(i <= n);
    return (sum);
}

int rec_fib(int n) {
    if(n <= 2) {
        return (1);
    }
    return (rec_fib(n - 1) + rec_fib(n - 2));
}


int testFunc(int x, int y) {

    const int ratio = 2;

    int temp;
    int inc;
    int result;

    scanf(result, temp);

    for(temp = 0; temp < 10; temp = temp + 1){
        scanf(inc);
        result = inc + result * ratio;
    }

    if(temp) {
        result = 2 + result;
        printf(result);
    } else {
        result = ratio * result;
    }

    do {
        temp = temp - 1;
        result = 2 + result;
    } while(temp < 5);


    temp = 5;

    return result / temp;

}

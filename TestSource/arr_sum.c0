
int arr[100];

int sum(int x) {
    int i, ret;
    for(i = 0; i < x; i = i + 1) {
        arr[i] = i + 1;
    }

    ret = 0;

    for(i = 0; i < x; i = i + 1) {
        ret = ret + arr[i];
    }

    return ret;

}

void main() {
    int x;
    x = sum(10);
    printf("sum is:", x);
}

// test recursive

int factWithPrint(int x) {
	printf("called", x * 2);
	if(x == 0) {
		return (1);
	} else {
		return (x * factWithPrint(x - 1));
	}
	return (1);

}

void main() {

	int x;
	scanf(x);
	printf(factWithPrint(x));

}

